(moving )
[x] adjust selecting
[x] negative space
// [] measuring

[x] fix zooming
[x] breaking points and connections
[x] undo / redo

[x] fix selection
[x] angles and lengths visible
[x] point placing and converting
[x] add point to shape
[x] chamfer
[x] rounding
    -  replace point with 2 new points, move them along 
[] boolean
    0. Zorg dat vormen of "normale vormen" of "Booleans" kunnen zijn.
    1. voor elke boolean check met bounding boxes met welke vormen ze mogelijk overlappen
    2. voor elke mogelijke overlappende vorm: loop voor zowel de boolean als de vorm door elk segment van de andere vorm, 
        vind alle intersections, split bijde vormen aan het einde op op basis van de intersections
    4. Onthoud lijst met intersections, bij 0 intersections stoppen we en voegen we de originele vorm aan de tekenpool toe.
    UNION
    5. maak een lijst van vorm-segmenten die buiten de boolean liggen.
    6. Voor de vorm, vind een segment buiten de boolean. Beloop de vorm, in een speci richting
        bij een intersectie switch van vorm naar boolean of omgekeerd. Behoud looprichting
    7. houd belopen segmenten bij. Als er eenmaal geloopd is, zijn de belopen segmenten de outline van de nieuwe vorm
    8. Als niet alle buiten-de-boolean segmenten in de nieuwe vorm zitten, start je het proces over om nog een vorm te vinden.
    9. Als er nieuwe vormen gevonden zijn, worden die aan de tekenpool toegevoegd, de originele vorm en boolean niet.
        vormen die niet betrokken waren bij booleans worden ook toegevoegd, booleans worden op een andere plek getekend
    10. er op na volgende booleans worden op de nieuwe tekenpool losgelaten, dus ook op nieuwe vormen
        boolean volgorde maakt uit!

    zorg dat booleans met meerder vormen werken
    zorg dat booleans met meerder gaten werken

[] copy / paste
[] curv comb
[] add square & circle
[] division of shapes + shape selection
[] code cleaning - break up process for debugging
[] save / load


[] auto obvious
[] good scaling/rotating visually
[] set mode for default placing whole or broken points, straight or cubic curve
[] knife

[] multiple letters
[] mirroring
[] skewing
[] jumping to points


[] sensible keymap

NOVA - test


voor selecteren:
    alleen punten
    max 50 punten per vorm
        selecteren met alle letters behalve j (kleine en hoofdletter)
    spatie ingedrukt voor selectmode?
    j en J + letter van punt om handles van punt te selecteren
    2 punten om een segment te selecteren en evt handles toe te voegen
    0 - 9 om shape te focussen (en letters van punten zichtbaar te maken)
    cijfer van gefocuste shape herhalen om hele shape te selecteren


voor rounding
    elk segment word in drieen verdeeld, waarbij de curv zo veel mogelijk behouden word.
    de buitenste delen van de segmenten laten we weg, vervolgens verbind je de binnenste delen aan elkaar.
    bij rounding is alles smooth verbonden, lengte van nieuwe handles nog uitzoeken, richting is hetzelfde als van handles van midden segmenten
    misschien aanpassen zodat altijd 2 handles
    misschien nieuwe class van 'simpele curves' waar alles naar gebakken wordt voor de rounding of boolean stap.